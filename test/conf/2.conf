#server list
server.1 = 127.0.0.1:9991
server.2 = 127.0.0.1:9992
server.3 = 127.0.0.1:9993

#admin port
client_port = 1236

#indcate myself
myid = 2

#debug
test_mode = 0

#if all renew failed, the lease can be hold during LEASE_TIMEOUT(seconds)
lease_timeout = 60

#the max time error(seconds) in nodes
d_max = 1

#when require timeout/failed, retry times
require_retry_times = 3

#require time out,before end 
#if get a msg which send *proposer_timeout* ago.
#just drop it
proposer_timeout = 40

#lead every *renew_internal* to renew lease
renew_internal = 30

